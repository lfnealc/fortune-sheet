import React from "react";

import RcTreeSelect from "rc-tree-select";

const TreeSelect: React.FC = () => {
  const returnNode = <RcTreeSelect />;

  return returnNode;
};

export default TreeSelect;
